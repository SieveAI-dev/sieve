# Sieve Dependabot 配置
# 文档：https://docs.github.com/en/code-security/dependabot/dependabot-version-updates
#
# 设计意图（供应链安全硬约束 + 可复现构建）：
# - pinned dependencies 是供应链安全承诺的硬约束
# - Dependabot 只动 patch / minor 升级；major 升级走人工评估
# - reproducible build 要求 Cargo.lock 入库，Dependabot PR 会更新 lockfile
# - open-pull-requests-limit 控制在 5 以内避免淹没
version: 2

updates:
  # ─────────────────────────────────────────────────────────────────────────
  # Cargo workspace 6 crate（v2.0 + v2.1 已落地）
  # ─────────────────────────────────────────────────────────────────────────
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "10:00"
      timezone: "Asia/Hong_Kong"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "rust"
    commit-message:
      prefix: "chore"
      prefix-development: "chore"
      include: "scope"
    # 供应链硬约束：major 升级必须人工评估（reproducible build 风险）
    ignore:
      - dependency-name: "*"
        update-types:
          - "version-update:semver-major"
    # 关键依赖列入"高风险升级"——高风险升级需人工 review
    # （Dependabot 不能直接打这个标，提示由 PR 模板 self-check 触发）
    groups:
      tokio-stack:
        patterns:
          - "tokio*"
          - "hyper*"
          - "rustls*"
      simd-stack:
        patterns:
          - "vectorscan-rs*"
      crypto-stack:
        patterns:
          - "sha2"
          - "ed25519*"
          - "bip39"
          - "bs58"
          - "hex"
          - "crc32fast"

  # ─────────────────────────────────────────────────────────────────────────
  # GitHub Actions（Week 1 sigstore + reproducible build pipeline 落地后生效）
  # ─────────────────────────────────────────────────────────────────────────
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 3
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "chore"
    # GitHub Actions major 升级（如 actions/checkout@v3 → v4）需评估对
    # reproducible build 的影响，Dependabot 不要自动开 PR
    ignore:
      - dependency-name: "*"
        update-types:
          - "version-update:semver-major"
