---
name: v2.0 Phase A 实现计划
description: PRD v2.0 HIPS 改造 Week 5 Phase A 任务拆解，4 个并行子代理 + 主会话收尾
type: plan
created: 2026-05-01
---

# v2.0 Phase A · Week 5 实现计划

> 来源：PRD v2.0 §10 Week 5 + §5.4/§5.5/§5.6/§6.3 + §9 #14/#16

## 总目标

落地 5 项 HIPS 升级能力的 Phase A 部分：
1. 用户规则系统（§5.5）→ `sieve-policy` 新 crate
2. 三态决策 ask + 灰名单 + Critical 锁（§5.4）→ IPC 协议扩展
3. 规则引擎 trait 扩展（§6.3）→ `MatchEngine` 加 `ScanRequest/Report` + `LayeredEngine`
4. 进程上下文反查（§5.6）→ `crates/sieve-cli/src/process_context.rs`
5. audit schema migration（§5.6.1）→ events 表加 `caller_pid` + `caller_exe`

## 并行子代理（Wave 1）

### A1 · sieve-policy crate + 引擎 trait 扩展

文件：新建 `crates/sieve-policy/`、改 `crates/sieve-rules/src/engine/mod.rs` + `lib.rs`、改根 `Cargo.toml`

范围：
- 新建 `sieve-policy` crate：loader / lint / engine / graylist 4 个模块
- 在 `sieve-rules/src/engine/mod.rs` 扩展 `ScanRequest` / `ScanReport` / `LayeredEngine`，向后兼容旧 `scan(&[u8])`

验收：`cargo build -p sieve-policy --locked`、`cargo test -p sieve-policy`、`cargo test -p sieve-rules` 全过

### A3 · 进程上下文反查模块

文件：新建 `crates/sieve-cli/src/process_context.rs` + 在 `main.rs` 注册模块

范围：macOS `proc_pidinfo` + `proc_pidpath` + LRU cache 30s TTL

验收：`cargo test -p sieve-cli --lib process_context`

### A4 · audit schema migration

文件：仅 `crates/sieve-cli/src/audit.rs` + 新测试

范围：events 表加 `caller_pid` / `caller_exe` 列；写 v1→v2 migration

验收：现有 audit 测试全过 + 新 migration 测试

### A5 · 三态决策 ask 协议扩展

文件：仅 `crates/sieve-ipc/src/protocol.rs`

范围：`DecisionRequest.allow_remember` + `DecisionResponse.context_hint`，全部 `#[serde(default)]` 兼容旧版

验收：`cargo test -p sieve-ipc`

## 主会话收尾（Wave 2）

- [ ] `sieve rules edit/list/disable/enable` CLI 子命令
- [ ] cargo fmt + clippy + test 全绿
- [ ] commit + 写 CHANGELOG
