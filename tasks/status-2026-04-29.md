---
name: 状态快照 2026-04-29
description: Phase 1 GA 前的功能完成度记录 + 后续工作入口
type: snapshot
---

# Sieve 项目状态快照

> 创建：2026-04-29
> 上一次 commit：`d8dd800` fix(ci): cargo-deny 改回 ubuntu-latest
> 代码体量：~20,000 行 Rust（5 crates）
> 工作树：clean

## 一句话总结

**代码 ~95% / 文档 100% / 商业化 0%**。Phase 1 GA 的硬阻塞已经从代码侧转移到商业基础设施侧（海外公司 / Stripe / License / .dmg / 域名），这些在 [doskey-todo.md](doskey-todo.md) 全部登记。

---

## 一、按功能维度的完成度

### 1. 核心检测能力（✅ 100%）

| 维度 | 状态 |
|------|------|
| 出站规则 OUT-01~12（含 OUT-06 ETH 地址替换 / OUT-12 BIP39 SHA-256） | ✅ 全部落地 |
| 入站 Critical IN-CR-01~06（含 IN-CR-04 持久化 9 条 fail-closed） | ✅ 全部落地 |
| 入站 General IN-GEN-01~06 | ✅ 全部落地 |
| FP 率（226 攻击 / 70 善意样本） | ✅ 0.0000%（PRD §9 #7 要求 < 0.5%） |
| Recall | ✅ 100% |
| P99 单请求延迟 | ✅ 24~104µs（预算 < 20ms） |
| Fuzz 三个 target（sse_parser / tool_use_aggregator / inbound_filter） | ✅ CI 60s 各跑一遍 |

### 2. 处置矩阵 + HIPS 弹窗（✅ 100%）

- 二维 disposition 矩阵（出站 / 入站 × 严重度）已落地（[ADR-016](../docs/design/ADR-016-disposition-matrix-2d.md)）
- `Disposition` enum（AutoRedact / GuiPopup / HookTerminal / StatusBar）+ `DefaultOnTimeout` enum（Redact / Block / Allow）路由完整
- `Action::HoldForDecision { request_id, timeout_seconds, default_on_timeout }` 已贯通 pipeline
- chain_depth 升级机制：HookMark+Redact 在 chain_depth ≥ 2 升级为 HoldForDecision；≥ 5 → 426

### 3. IPC + 双层防御（✅ 95%）

- `sieve-ipc` JSON-RPC over Unix socket + 文件锁 pending/decisions ✅
- `sieve-hook` 极简二进制（启动 < 50ms）+ 心跳 + heuristic fail-closed ✅
- X-Sieve-Origin header（chain_depth + Ed25519 签名 4 段格式）✅
- **缺**：GUI 客户端在独立仓库 `sieve-gui-macos`，不在本 workspace（[ADR-012](../docs/design/ADR-012-native-gui-app.md)）

### 4. 多 agent 适配（✅ 95%）

- `AgentAdapter` trait（ClaudeAdapter / OpenClawAdapter / HermesAdapter）✅
- OpenAI Chat Completions 协议双协议路由（`/v1/messages` 与 `/v1/chat/completions`）✅
- OpenClaw 多 provider 路由（`X-Sieve-Provider` header + `upstream_routes.json`）✅
- Hermes 流式适配 ✅
- **缺**：OpenClaw/Hermes 各 3 个 SPEC-004 §10 TBD 等 Week 8 真实 dogfood 验证

### 5. 部署链路（70%）

| 环节 | 状态 |
|------|------|
| `sieve setup` 一键安装（embedded_rules + sieve.toml + launchd plist + sentinel） | ✅ |
| `sieve setup --agent / --all-detected / --dry-run / --yes` | ✅ |
| `sieve doctor`（真实 adapter 检查 + 失败 exit 1） | ✅ |
| `sieve uninstall`（按 agent_filter + setup.log 回滚） | ✅ |
| .dmg 打包 + macOS notarization | ❌ 阻塞 R10-#3（绝对路径） |
| GUI App | ❌ 独立仓库 doskey 单做 |

### 6. 透明性 / 供应链（80%）

- sigstore CI pipeline ✅（fmt/clippy/test/deny/fuzz-quick 全跑通）
- reproducible build 脚本 ✅
- LICENSE / SECURITY.md / CODEOWNERS ✅
- **缺**：repo 公开（Week 12 GA 不可逆动作，[ADR-011](../docs/design/ADR-011-pre-ga-private-repo.md)）
- **缺**：rules 真实公钥 / Apple Developer 证书

### 7. 测试质量（✅ 100%）

- 479 tests 全部 PASS（含 unit / integration / contract / fuzz seeds / dataset_fp_rate）
- benchmark 全部满足或超出 PRD §6.6 性能预算
- 已知 flaky：`outbound_block` ~1/13（daemon startup race，单次重试可过）

### 8. 文档（✅ 100%）

- PRD v1.5（多 agent 扩展）✅
- 19 个 ADR ✅
- 4 个 SPEC（hook 协议 / HIPS 行为 / sieve setup / multi-agent setup）✅
- 3 篇推广文章草稿（中转站揭黑 / 自验证 / UCSB 论文）✅
- landing page 单文件（45KB）✅

### 9. 商业化（⏳ 0%）

完全卡 doskey 本人动作，详见 [doskey-todo.md](doskey-todo.md) 17 条：

- 海外公司注册（HK / SG / Stripe Atlas）
- Stripe / Coinbase Commerce 商户接入
- License 后端（域名 + 部署 + DB）
- 域名 + DNS + 邮箱
- Apple Developer Program + .dmg 公证
- KOL 接洽（Chaofan / 慢雾 / Yu Feng）
- Discord 闭测频道

### 10. 内容 / 营销（80%）

- 文章 1/2/3 中文草稿 ✅
- landing page 第一版 ✅
- **缺**：doskey 润色 + 配 placeholder + 配 DNS + 发布

---

## 二、剩余 known issues

唯一未修：**R10-#3** sieve-hook 绝对路径，等 .dmg 打包路径拍板后再修。

其余 56 条 codex review findings 已修 55 条，详见 [known-issues-v1.4.md](known-issues-v1.4.md)。

---

## 三、继续工作的入口

1. **接着修代码**：看 [known-issues-v1.4.md](known-issues-v1.4.md) 唯一剩余 R10-#3
2. **doskey 自己推**：[doskey-todo.md](doskey-todo.md) 17 条
3. **再来一轮 review**：跑 `codex` 看是否还能挑出 P0/P1
4. **进入 Week 8 dogfood**：把 OpenClaw / Hermes 装上跑 SPEC-004 §10 6 个 TBD
5. **Week 12 GA 准备**：sigstore + reproducible build 真实跑一遍 → 比对 hash → repo public

---

## 四、关键命令速查

```bash
# 全量验证
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo test --workspace --locked
cargo deny check

# benchmark + FP 率
cargo bench -p sieve-rules
cargo test -p sieve-rules --test dataset_fp_rate --release

# fuzz 60s 各 target
cargo +nightly fuzz run sse_parser -- -max_total_time=60
cargo +nightly fuzz run tool_use_aggregator -- -max_total_time=60
cargo +nightly fuzz run inbound_filter -- -max_total_time=60

# 启动透传 daemon
RUST_LOG=info cargo run -p sieve-cli -- start --config sieve.toml

# 一键安装（dry-run）
cargo run -p sieve-cli -- setup --all-detected --dry-run
```
