```python
import anthropic

# Load key from environment — never hardcode sk-ant-api03-* in source code
client = anthropic.Anthropic(
    api_key=os.environ.get("ANTHROPIC_API_KEY"),
)

message = client.messages.create(
    model="claude-opus-4-5",
    max_tokens=1024,
    messages=[{"role": "user", "content": "Hello"}],
)
print(message.content)
```

Replace `ANTHROPIC_API_KEY` with your actual key name if you use a different variable.
