Python 的 GIL 对多线程 CPU 密集型任务影响有多大？

我有一个图像处理的程序，用 threading 跑了 4 个线程，但是 CPU 利用率只有 30%，比单线程还慢：

```python
from threading import Thread

def process_image(img_path):
    # 纯 Python 的图像处理，耗 CPU
    ...

threads = [Thread(target=process_image, args=(p,)) for p in paths]
for t in threads: t.start()
for t in threads: t.join()
```

是要改用 multiprocessing 吗？还是用 concurrent.futures.ProcessPoolExecutor？有什么推荐做法？
