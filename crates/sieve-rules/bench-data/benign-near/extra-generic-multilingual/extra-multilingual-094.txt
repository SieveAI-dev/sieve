CSS grid 搞不明白 😵

想实现一个 3 列布局，中间列固定宽度，左右两列自适应：

```css
.layout {
  display: grid;
  grid-template-columns: 1fr 680px 1fr;
  gap: 24px;
}
```

手机端这样不行，中间列 680px 超出屏幕了。

要怎么做响应式？用 `min()` 函数还是 media query？`clamp(320px, 680px, 100%)` 这样写对吗？
