pnpm workspace monorepo 里共享 tsconfig，子包的 path alias 不生效：

根目录 tsconfig.json 里定义了：
```json
{
  "compilerOptions": {
    "paths": {
      "@shared/*": ["./packages/shared/src/*"]
    }
  }
}
```

在 packages/app 里 import from '@shared/utils' 编译报错，说找不到模块。

子包的 tsconfig 有 extends 根目录的配置，但 paths 里的相对路径是相对于哪个文件算的？需要在子包里重新定义 paths 吗？
