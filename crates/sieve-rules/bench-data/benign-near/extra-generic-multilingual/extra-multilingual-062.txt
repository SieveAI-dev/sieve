please help my zustand store isn't persisting

```typescript
const useStore = create(
  persist(
    (set) => ({
      user: null,
      setUser: (user) => set({ user }),
    }),
    { name: 'user-storage' }
  )
);
```

on page refresh the state resets to null. i thought persist was supposed to save to localStorage? using next.js app router

is this a SSR hydration issue?
