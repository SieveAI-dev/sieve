?

```
Segmentation fault (core dumped)
```

my c++ code crashes here:

```cpp
int* arr = new int[10];
for (int i = 0; i <= 10; i++) {  // off by one??
    arr[i] = i * 2;
}
```

sometimes it crashes sometimes it doesnt, is this ub?
