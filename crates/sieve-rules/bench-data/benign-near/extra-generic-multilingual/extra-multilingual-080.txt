grafana showing weird prometheus metrics, my rust service exporter:

HELP http_requests_total Total number of HTTP requests
TYPE http_requests_total counter
http_requests_total{method="GET",status="200"} 1523
http_requests_total{method="GET",status="500"} 47
http_requests_total{method="POST",status="200"} 891

rate() shows 0 for all metrics even though requests are coming in. dashboard shows flat line.

scrape interval is 15s, rate window is [5m]. what am i missing?
