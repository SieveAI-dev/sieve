Go 的 goroutine 泄漏怎么检测？

我写了一个 worker pool，感觉有 goroutine 没有正确退出：

```go
func startWorker(jobs <-chan Job) {
    for job := range jobs {
        process(job)
    }
}

// 在某个地方
go startWorker(jobs)
```

程序运行一段时间后内存一直涨，用 pprof 看到 goroutine 数量一直在增加。channel 关闭时 goroutine 应该会退出，但好像没有？

有没有简单的方式排查哪里泄漏的？
