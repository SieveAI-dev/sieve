wasm-bindgen 导出的函数怎么处理 Rust 的 Result 类型？

```rust
#[wasm_bindgen]
pub fn parse_hex(input: &str) -> Result<Vec<u8>, JsError> {
    hex::decode(input).map_err(|e| JsError::new(&e.to_string()))
}
```

JS 侧调用这个函数，如果 Rust 返回 Err，JS 这边会抛 exception 吗？还是要用 try/catch？

另外，返回 Vec<u8> 性能怎么样？有没有零拷贝的方式把数据传给 JS？
