my regex isnt matching and idk why

```python
import re
pattern = "\d+"
text = "abc 123 def"
result = re.match(pattern, text)
print(result)  # None
```

why is it None?? there are digits in the string
tried re.search too but getting weird results
