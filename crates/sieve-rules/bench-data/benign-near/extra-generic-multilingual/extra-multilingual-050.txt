this is brok pls help

```solidity
function withdrawl(uint amount) external {
    require(balances[msg.sender] >= amount);
    balances[msg.sender] -= amount;
    payable(msg.sender).transfer(amount);
}
```

getting "revert" when trying to withdraw, the balance is definitely there i checked
also is there a reentrancy issue here or am i overthinking
