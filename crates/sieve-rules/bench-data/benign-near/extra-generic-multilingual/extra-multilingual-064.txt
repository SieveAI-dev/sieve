这报错是啥意思？

borrow of moved value: `config`
  --> src/main.rs:15:20
   |
12 |     let config = load_config();
   |         ------ move occurs because `config` has type `Config`, which does not implement the `Copy` trait
13 |     process(config);
   |             ------ value moved here
15 |     log(config.name);
   |         ^^^^^^ value borrowed here after move

我就是想在两个地方用同一个 config，这有什么问题吗
