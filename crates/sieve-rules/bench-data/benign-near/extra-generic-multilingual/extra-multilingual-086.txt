asyncio.gather 的写法我试了，确实并发了，谢谢。

但是现在有个新问题：其中一个 task 抛异常，其他的 task 好像也被取消了，这是预期行为吗？

```python
results = await asyncio.gather(
    fetch_users(),
    fetch_orders(),  # 这个抛了异常
    fetch_products(),
)
```

我希望失败的 task 不影响其他 task，有 return_exceptions=True 这个参数但不确定用法。
