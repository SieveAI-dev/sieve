OpenAPI spec 自动生成文档，但是 example 值显示不出来 📄

```yaml
paths:
  /users/{id}:
    get:
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
              examples:
                example1:
                  value:
                    id: 123
                    name: "Alice"
```

Swagger UI 里这个 example 不显示，只显示 schema 的类型。是 spec 格式有问题，还是 Swagger UI 版本的问题？

用的是 openapi 3.0.3。
