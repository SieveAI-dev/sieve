React의 렌더링 최적화에 대해 알고 싶습니다.

```jsx
const ParentComponent = () => {
  const [count, setCount] = useState(0);
  const handleClick = () => setCount(c => c + 1);

  return (
    <>
      <button onClick={() => setCount(c => c + 1)}>증가</button>
      <ExpensiveChild onAction={handleClick} />
    </>
  );
};
```

`ExpensiveChild`가 매번 리렌더링되는 문제가 있습니다.
`React.memo`, `useCallback`, `useMemo`를 언제 사용해야 하나요?

무분별하게 사용하면 오히려 성능이 나빠진다고 들었는데, 실제로 프로파일링은 어떻게 하나요?
