WebSocket connection drops after exactly 60 seconds every time 🔌

Client (browser) connects to my Rust tokio-tungstenite server. Works fine for exactly 60s then disconnects with:

```
WebSocket is closed before the connection is established.
code: 1006, reason: ""
```

Nginx sits in front of the server. Both client and server code look correct, messages exchange fine during the 60s window.

Is this an nginx proxy_read_timeout? I have `proxy_read_timeout 60;` in my config. Should it be `proxy_read_timeout 3600;` + `proxy_send_timeout 3600;`? Also need keepalive pings?
