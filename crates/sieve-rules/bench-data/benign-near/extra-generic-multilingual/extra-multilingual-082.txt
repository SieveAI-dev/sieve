tried the Arc<RwLock<>> approach you suggested, but now i'm getting:

cannot borrow data in an `Arc` as mutable
help: trait `DerefMut` is required to modify through a dereference

my updated code:
```rust
let shared = Arc::new(RwLock::new(vec![1, 2, 3]));
let clone = Arc::clone(&shared);
tokio::spawn(async move {
    clone.write().push(4);  // error here
});
```

am i calling write() wrong? do i need to await it for tokio's RwLock?
