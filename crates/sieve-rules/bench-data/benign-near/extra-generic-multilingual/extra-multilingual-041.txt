非同期Rustでのエラー伝播パターンについて質問があります。

```rust
async fn process() -> Result<String, Box<dyn std::error::Error>> {
    let data = fetch_data().await?;
    let parsed = parse_data(&data)?;
    Ok(format!("Result: {}", parsed))
}
```

`?`演算子はasync関数でも同じように使えますか？

また、複数の異なるエラー型を返す可能性がある場合、`Box<dyn Error>`とカスタムエラー型（`thiserror`使用）どちらが良いですか？ライブラリクレートとバイナリクレートで使い分けるべきですか？
