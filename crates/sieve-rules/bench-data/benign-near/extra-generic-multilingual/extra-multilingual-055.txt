not sure if this is a bug or intended

```rust
let x = 5;
let y = &x;
let z = *y + 1;
println!("{}", x); // still works?
```

i thought after taking a reference x would be moved or something. why can i still use x after creating a reference to it?

also whats the difference between & and &mut
