console.lo(x) is erroring

Uncaught TypeError: console.lo is not a function

oh wait lol i see it. but also the variable x is undefined even tho i set it:

```js
var x;
if (condition) {
    var x = 5;
}
console.log(x); // undefined sometimes
```

is this a var hoisting thing?
