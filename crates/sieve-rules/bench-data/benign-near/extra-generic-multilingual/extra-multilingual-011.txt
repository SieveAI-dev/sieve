Docker compose 里服务启动顺序怎么控制？

```yaml
services:
  app:
    image: myapp:latest
    depends_on:
      - db
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: myapp
```

我知道 depends_on 只保证容器启动顺序，不保证服务 ready。但是 app 启动的时候 postgres 还没初始化完，connection refused 报错。

有没有比 sleep 5 更优雅的方式等 postgres ready？
