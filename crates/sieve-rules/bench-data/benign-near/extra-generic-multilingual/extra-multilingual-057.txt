y is my component not re-rendering

```jsx
const [items, setItems] = useState([1, 2, 3]);

const addItem = () => {
    items.push(4);  // mutating state directly
    setItems(items);
};
```

i call setItems but nothing updates on screen. is this a react bug?

(using react 18 btw)
