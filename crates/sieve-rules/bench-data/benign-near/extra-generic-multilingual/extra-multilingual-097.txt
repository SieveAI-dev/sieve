Makefile의 자동 의존성 추적을 이해하고 싶습니다 🛠️

```makefile
SRCS = src/main.c src/utils.c
OBJS = $(SRCS:.c=.o)

myapp: $(OBJS)
	$(CC) -o $@ $^

%.o: %.c
	$(CC) -c -o $@ $<
```

헤더 파일을 수정해도 재컴파일이 트리거되지 않습니다. `-MMD -MP` 플래그로 `.d` 파일을 생성하는 방법이 있다고 들었는데, Makefile에 어떻게 통합하나요?
