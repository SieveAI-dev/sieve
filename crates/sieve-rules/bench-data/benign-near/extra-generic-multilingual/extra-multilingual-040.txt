Wie implementiere ich eine sichere Authentifizierung mit JWT in einer Rust-API?

```rust
use jsonwebtoken::{encode, decode, Header, Algorithm, Validation, EncodingKey, DecodingKey};

#[derive(Serialize, Deserialize)]
struct Claims {
    sub: String,
    exp: usize,
}
```

Meine Fragen:
- Soll ich HS256 oder RS256 verwenden? Was sind die Vor- und Nachteile?
- Wie speichere ich den Secret Key sicher in Produktion?
- Token-Refresh-Strategie: Sliding window oder feste Ablaufzeit?
- Wie invalidiere ich ein Token vor Ablauf (z.B. bei Logout)?
