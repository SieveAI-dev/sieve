RustのOption型の使い方について教えてください。

```rust
fn find_user(id: u32) -> Option<User> {
    // データベースからユーザーを検索
    users.iter().find(|u| u.id == id).cloned()
}

fn main() {
    let user = find_user(42);
    // user.nameにアクセスしたい
}
```

`if let`と`match`と`unwrap_or`はどう使い分けるのでしょうか？
また、`Option<Option<T>>`が出てきた場合は`flatten()`を使えばいいですか？
