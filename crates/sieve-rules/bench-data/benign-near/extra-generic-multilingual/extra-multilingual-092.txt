SQL window function 用法不确定 🤷

我想给每个用户的订单按时间排序加个序号：

```sql
SELECT
  user_id,
  order_id,
  created_at,
  ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY created_at) as order_seq
FROM orders;
```

这个写法对吗？PARTITION BY 是按 user_id 分组，ORDER BY 是在每个分组内排序，理解对吗？

如果我想要的是每个用户的第一笔订单怎么筛选？用 WHERE order_seq = 1 还是有别的方式？
