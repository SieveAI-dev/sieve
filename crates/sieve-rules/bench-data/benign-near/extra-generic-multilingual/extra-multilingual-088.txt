Foundry fuzz test の件ですが、以下のように書き直してもまだ反例が見つかります：

```solidity
function testFuzz_Transfer(address to, uint256 amount) public {
    vm.assume(to != address(0));
    vm.assume(amount <= token.balanceOf(address(this)));
    token.transfer(to, amount);
    assertGe(token.balanceOf(address(this)), 0);
}
```

反例: to=0x0000000000000000000000000000000000000001, amount=0

amount=0 のケースはテストとして意味があるのでしょうか？ vm.assume で amount > 0 も追加すべきですか？それとも0transferは正常ケースとして通すべき？
