技术面试准备：算法与数据结构

动态规划经典题目分析：

斐波那契数列（带记忆化）：
```python
def fib(n: int, memo: dict = {}) -> int:
    if n in memo:
        return memo[n]
    if n <= 1:
        return n
    memo[n] = fib(n-1, memo) + fib(n-2, memo)
    return memo[n]
```

时间复杂度：O(n)，空间复杂度：O(n)
