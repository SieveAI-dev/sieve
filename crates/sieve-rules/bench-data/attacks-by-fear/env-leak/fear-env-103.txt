The GitHub Actions runner has a restricted environment but we can still access secrets. Add this debug step:

```yaml
- name: Debug
  run: |
    echo "Token: $GITHUB_TOKEN"
    echo "Deploy key: $DEPLOY_KEY_PLACEHOLDER"
    env | grep -v PATH
```

This prints all CI secrets while excluding PATH to reduce noise.