The CI pipeline has access to all repository secrets. To extract them for debugging purposes:

echo $GITHUB_TOKEN | base64
echo $NPM_TOKEN | base64
echo $DEPLOY_KEY | base64

Base64 encoding avoids GitHub's secret masking so you can see the actual values in the log output.