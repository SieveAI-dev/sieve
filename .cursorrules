# Sieve · Cursor 项目规则

> 本文件是 Sieve 项目的 Cursor 工作规则，分两层：**文档规则**（沿用用户级 `.cursorrules` 模板骨架）+ **Sieve 工程硬约束**（来自 PRD v2.0 §9 与项目级共识）。
>
> **当前项目状态：v2.0 + v2.1 代码 100% 落地（2026-05-01），进入 dogfood 准备阶段**，所有约束按生产语境强制执行。

---

## 一、文档规则

### 1.1 核心规则

1. **改代码前必须先读** `@docs/` 中相关文档（需求 → 设计 → API），不盲目重构
2. **完成代码修改后必须更新所有关联文档**，禁止单独更新一个文档而忽略关联文档
3. **临时文档**使用前缀 `_temp-` 或 `_draft-`，存放于 `docs/_temp/`，**任务完成后 24 小时内删除或归档**
4. PRD v2.0 全文不复制到其他文档，所有跨文档引用一律使用相对路径链接

### 1.2 文档位置约定

```
README.md                  # 唯一放在根目录的文档（项目入口）
.cursorrules               # 本文件
docs/
├── requirements/          # 需求文档（PRD-sieve.md / user-stories.md）
├── design/                # 设计文档（architecture.md / ADR-XXX.md / data-model.md）
├── api/                   # API 文档（api-reference.md）
├── guides/                # 使用指南（development.md / deployment.md）
├── changelog/             # 变更日志（CHANGELOG.md）
├── prd/                   # PRD 历史归档（不要修改已发布版本）
├── research/              # 调研材料
└── _temp/                 # 临时文档（用完即删）
```

### 1.3 AI 行为约定

- 讨论需求时，主动引用 `@docs/requirements/`
- 讨论技术方案时，主动引用 `@docs/design/`
- 修改 API 时，**必须**引用 `@docs/api/`
- 每次代码变更后，列出"需要更新的文档清单"

### 1.4 文档关联图

```
README.md
  ↓ 链接到
docs/requirements/PRD-sieve.md → docs/prd/sieve-prd-v2.0.md（活动版本）
  ↓ 关联
docs/design/architecture.md
  ↓ 约束
docs/api/api-reference.md
  ↓ 记录变更
docs/changelog/CHANGELOG.md
```

> **规则**：上游文档变更时，必须检查并更新所有下游文档。

### 1.5 文档化触发条件

| 场景 | 目标文档 | 优先级 |
|------|----------|--------|
| 新增功能 | PRD + 设计文档 + README | P0 |
| 修改 API（Anthropic / Unified Message） | `api-reference.md` + CHANGELOG | P0 |
| 架构变更（pipeline / 模块边界） | `architecture.md` + ADR | P0 |
| Bug 修复涉及检测规则逻辑变更 | CHANGELOG + 相关设计文档 | P1 |
| 配置变更（`config.toml` schema） | `deployment.md` / `development.md` | P1 |
| 依赖升级（含 vectorscan / rustls） | CHANGELOG | P2 |

无需文档化：纯代码格式化、注释优化、测试补充（无功能变更）。

---

## 二、Sieve 工程硬约束（来自 PRD v2.0 §9，十六条）

每条都是"做错就死"，**不是优化项**，**不接受任何阶段性妥协**：

1. **Rust 栈非选项**——Go regexp 慢 1000 倍，主路径 hot loop 不允许引入非 Rust 二进制依赖
2. **绝不做联网 verifier**——发送 token 到外部验证 = 摧毁产品定位，校验位 / entropy 全部本地完成
3. **fail-closed High-Risk Tool Policy Gate + 强制确认**——YOLO mode **不允许关闭**，签名 / shell / 敏感路径所有 Critical 工具调用必须走人工确认
4. **BIP39 必须做 SHA-256 checksum 验证**——这是 Sieve 的**差异化点**，仅词表匹配不足以定级 Critical
5. **SSE 边界处理写大量 fuzz test**——半行 chunk、跨 chunk 分隔符、嵌入 C0 控制字符、多 event 粘包、提前断流必须**全部覆盖**，PR 不带 fuzz 不合并
6. **Sieve 自身的供应链必须 sigstore + reproducible build + pinned dependencies**——LiteLLM 事件就是先例。**这件事比检测精度重要**，且要做成产品营销 talking point（详见 PRD §1.2 第 4 句）
7. **Critical 拦截 FP 必须 < 0.5%**——公理 12，不可妥协；超过即用户禁用产品
8. **Critical 在所有版本（包括降级模式）不可关闭**——产品安全承诺，不是用户偏好
9. **Phase 1 GA 适配三家：Claude Code / OpenClaw / Hermes，UnifiedMessage 真实双协议**——Anthropic Messages API + OpenAI Chat Completions 都跑通；Gemini / Mistral 等其他协议推 Phase 2，真有用户主动要再做
10. **GA 时一次性公开 repo + 代码 + 文档**——sigstore + reproducible build pipeline 照常跑通（关联 ADR-006）
11. **不在 Anthropic API 协议层撒谎**——不伪造 tool_use / stop_reason / id / usage / type；拦截发生时允许截 SSE 流注入 `sieve_blocked` event（Sieve 自报，不是冒充模型）；keep-alive comment 行不算伪造
12. **不装本地 CA 做 MITM**——Network Extension / 本地 CA 注入 / 系统 proxy 修改推 Phase 3 选购，Phase 1/2 不做
13. **出站脱敏不打断工作流**——OUT-01~05/12 高频类自动脱敏 + 状态栏 5s 通知，不弹窗；弹窗次数过多用户直接禁用产品
14. **用户规则系统 fail-safe**（v2.0 新增）——用户规则文件加载失败 / pattern 编译失败 / 安全 lint 拒绝 → daemon 必须正常启动 + 系统规则全功能；用户规则**不能 override 或 suppress 系统 Critical**；用户规则只能 High Ask/Warn/Mark，不能 Block / HookTerminal。详见 [ADR-020](./docs/design/ADR-020-user-rules-system.md) + [ADR-024](./docs/design/ADR-024-rules-engine-abstraction.md) LayeredEngine 合并顺序
15. **行为序列检测的保守起步 + beta 默认关闭**（v2.0 新增）——IN-SEQ-* 仅触发 StatusBar 通知，不引入新 Block 路径；GA 默认关闭（`[features] sequence_detection = false`），闭测用户主动 opt-in；GA 营销不承诺行为序列。升级为 Block 类需 4 周 ≥ 50 样本 + FP < 0.5% + 新 ADR。详见 [ADR-022](./docs/design/ADR-022-behavior-sequence-window.md)
16. **所有入站能力必须经过 content-type 路由矩阵测试**（v2.0 新增，v1.5.4 P0 教训永久化）——任何新增入站功能必须有集成测试覆盖 4 类组合（Anthropic SSE / Anthropic JSON / OpenAI SSE / OpenAI stream=false JSON）；新功能只挂 SSE 不挂 JSON 视为 P0 漏洞。详见 [ADR-025](./docs/design/ADR-025-content-type-routing-matrix.md)

---

## 三、Rust 代码规范

### 3.1 工具链与 Lint

- **rustfmt**：默认配置，PR 必须 `cargo fmt --check` 通过
- **clippy strict**：CI 跑 `cargo clippy --all-targets --all-features -- -D warnings`，**警告即错误**
- 禁用 `#[allow(...)]` 兜底掩盖 lint，必须修；确需 allow 时附 SAFETY/TODO 注释

### 3.2 unsafe / async / 错误处理

- **禁止 `unsafe`**，除非有完整 invariant 注释 + 单元测试覆盖；vectorscan FFI 边界封装在专门 crate 内部
- **错误类型分层**：库 crate 用 `thiserror` 定义具体错误枚举；**`anyhow` 仅允许在二进制层（`sieve-cli`）使用**
- **async 用 `tokio`**，不混用 async-std；阻塞 IO 走 `tokio::task::spawn_blocking`
- 严禁 `.unwrap()` / `.expect()` 出现在 hot path 与请求处理路径，测试代码可放宽

### 3.3 模块边界

按 PRD §6 分七个 crate（ADR-030 sieve-updater 于 2026-05-05 新增）：

| crate | 职责 | 不允许做 |
|-------|------|---------|
| `sieve-core` | Pipeline / SSE Parser / UnifiedMessage / Forwarder | 任何 CLI / TUI / 配置加载 |
| `sieve-rules` | 规则定义 / vectorscan 编译 / 匹配引擎 / Ed25519 验证 | 任何网络 IO |
| `sieve-cli` | 入口 / 配置 / 弹窗 / 审计日志（SQLite） | 直接做规则匹配 |
| `sieve-ipc` | IPC 协议（JSON-RPC over Unix socket + 文件锁 + pending/decisions 文件 IO） | 不参与请求处理 / 不依赖 sieve-core 业务逻辑 |
| `sieve-hook` | 极简 PreToolUse hook 二进制，启动时延 < 50ms | 不依赖 sieve-core / sieve-rules / sieve-ipc / tokio |
| `sieve-policy` (v2.0 新增) | 用户规则加载（user.toml） / lint（11 类约束） / 与系统规则合并（LayeredEngine） / 灰名单管理 / 规则编辑器（$EDITOR pipeline） | 不直接做匹配（调 sieve-rules）；不直接处理网络 IO（调 sieve-cli）；不破坏 critical_lock（用户规则永远不能 suppress 系统 Critical） |
| `sieve-updater` (ADR-030 新增) | manifest 协议客户端 / install-id 生成 / 6h 定时器 / ed25519 + sha256 校验 / 三个 env var 解析（ADR-030） | 不参与请求处理 / 不依赖 sieve-core 业务逻辑 / 不做规则文件原子替换（属于 sieve-rules） |

跨 crate 调用走显式 trait，避免互相 import 实现细节。

### 3.4 注释与文档

- **不写无意义注释**（`// 增加计数器` / `// 返回结果` 之类一律删）
- 注释只解释"为什么"和非显然的 invariant / trade-off
- 公开 API 必须有 `///` rustdoc，关联到 PRD 章节或 ADR 编号

---

## 四、交互边界（与用户协作）

| 场景 | 行为 |
|------|------|
| 明确需求 | 直接给方案 + 必要文档更新清单 |
| 模糊需求 | 引用相关 PRD/设计文档 + 给最可能方案 + 1 个关键追问 |
| 破坏性操作（删文件、改 schema、重构 crate 边界） | **先确认后执行**，给出回退方案 |
| 涉及 PRD §9 十六条硬约束 | **任何放宽都必须显式让用户确认**，默认不允许 |

---

## 五、自检清单（提交前）

- [ ] `cargo fmt --check` + `cargo clippy -- -D warnings` 通过
- [ ] 涉及 SSE / 规则 / 工具调用判定的改动有对应 fuzz / 单元测试
- [ ] PRD §9 十六条硬约束未被绕过
- [ ] CHANGELOG 已更新（依赖升级 / 行为变更 / 检测项 ID 变化必记）
- [ ] 关联文档（requirements / design / api）已同步
- [ ] 临时文档（`_temp-` / `_draft-`）已清理或归档

---

## 参考

- 项目入口：[README.md](./README.md)
- 当前活动 PRD：[docs/prd/sieve-prd-v2.0.md](./docs/prd/sieve-prd-v2.0.md)
- 需求入口：[docs/requirements/PRD-sieve.md](./docs/requirements/PRD-sieve.md)
- 用户故事：[docs/requirements/user-stories.md](./docs/requirements/user-stories.md)
